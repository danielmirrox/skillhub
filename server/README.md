# Server — SkillHub

Node.js + Express + PostgreSQL. API, Auth, YandexGPT.

**Ответственный:** Даниэл (Backend)

```bash
npm install
npm run dev
```

.env: `DATABASE_URL`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `YANDEXGPT_API_KEY`
